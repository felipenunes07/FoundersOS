// Entry das páginas de conteúdo estáticas. Elas não usam React — o HTML já vem
// pronto no fonte, que é o formato que o Google indexa melhor. Este arquivo só
// existe para o Vite processar e versionar o CSS dos guias.
import "./guia.css";
